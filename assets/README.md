# assets/

| File | What |
| ---- | ---- |
| `bts_pipeline.svg` / `bts_pipeline.png` | the B-T-S pipeline architecture figure in the README — generated by `python scripts/draw_pipeline.py` (PNG needs `cairosvg`) |
| `figures/*.png` | figures of the v6.4 reference run. `ablation_decoding`, `oracle_vs_k`, `stage1_weight_surface`, `fluency_gate_and_weight_surface` and `per_day_wer` are the notebook's own outputs; `joint_sweep` and `error_analysis` are drawn by `src/analysis.py` from `results/joint_sweep_all_combos.csv` and `results/error_analysis_search_vs_model.csv` |

Figures produced by a new run go to `figures/` (gitignored), not here.
