# Core deep learning
torch>=2.1
numpy>=1.24
scipy>=1.10

# Neural-data IO
h5py>=3.8
pandas>=2.0

# Metrics
editdistance>=0.6

# Beam search: flashlight-text is used DIRECTLY (not through
# torchaudio.models.decoder.ctc_decoder, which is not guaranteed on new torch
# images). Output was verified identical to the torchaudio wrapper.
flashlight-text>=0.0.7
kenlm>=0.2            # pip install https://github.com/kpu/kenlm/archive/master.zip if this fails

# LLM rescoring (QLoRA next-word-prediction fine-tune + scoring)
transformers>=4.44
accelerate>=0.33
peft>=0.11
bitsandbytes>=0.43

# Memory guard + reporting
psutil>=5.9
matplotlib>=3.7
tqdm>=4.66

# Optional: only needed if resolving competition data / datasets via kagglehub
kagglehub>=0.3

# Optional: only to regenerate assets/bts_pipeline.png (scripts/draw_pipeline.py)
# cairosvg>=2.7
