# Core deep learning
torch>=2.1
torchaudio>=2.1          # provides the flashlight-backed CTC beam-search decoder

# Neural-data IO & signal processing
h5py>=3.8
numpy>=1.24
pandas>=2.0
scipy>=1.10

# Metrics
jiwer>=3.0
editdistance>=0.6

# Beam search backend (flashlight-text powers torchaudio.models.decoder.ctc_decoder)
flashlight-text>=0.0.7

# LLM rescoring (4-bit quantized Qwen2.5-7B)
transformers>=4.44
accelerate>=0.33
bitsandbytes>=0.43

# Utilities
tqdm>=4.66
matplotlib>=3.7

# Optional: only needed if resolving datasets via kagglehub instead of local paths
kagglehub>=0.3
